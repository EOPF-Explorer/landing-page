{
  "name": "eopf-explorer-landing-page",
  "version": "0.0.0",
  "type": "module",
  "devDependencies": {
    "@eox/pages-theme-eox": "^0.10.0"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "serve": "vitepress serve",
    "format": "npx prettier --write **/*.{js,css,vue}"
  }
}
